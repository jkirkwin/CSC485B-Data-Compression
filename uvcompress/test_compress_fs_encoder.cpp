#include "compress_fs_encoder.h"
#include "binary_field.h"
#include <cassert>
#include <iostream>


void testNoInput() {
    assert(false); // todo
}

void testEmptyInput() {
    assert(false); // todo
}

void testOneBit(FSEncoder fsEncoder) {
    assert(false); // todo
}

void testOneByte(FSEncoder fsEncoder) {
    assert(false); // todo
}

void testOverOneByte(FSEncoder fsEncoder) {
    assert(false); // todo
}

void testTwoBytes() {
    assert(false); // todo
}

// todo refactor this (and update local env) to use boost test lib
int main() {
    std::cout << "FSEncoder -- Running Tests." << std::endl;

    std::cout << "FSEncoder -- All Tests Passed." << std::endl;
}